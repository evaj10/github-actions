name: Ime Workflow-a

on: [push] 

jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "Hello ovo je ubuntu komanda"
      - name: multiline script
        run: |
          node -v
          npm -v
          echo "Vise komandi u jednom koraku"